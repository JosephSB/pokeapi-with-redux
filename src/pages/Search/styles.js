import styled from "styled-components";

export const StyledContainerSearchs = styled.div`
    width: 100%;
    max-width: 900px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)) ;
    justify-content: center;
    align-items: center;
    gap: 0.83rem;
`